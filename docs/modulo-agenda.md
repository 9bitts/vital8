# Módulo Agenda

Grade por profissional, conflitos, confirmação por link, recepção, painel TV.

## Rotas

- `/app/agenda`, `/app/recepcao`

## Multi-unidade

Filtro por `branchId` no header quando unidade selecionada.
