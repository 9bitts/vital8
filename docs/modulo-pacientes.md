# Módulo Pacientes

Cadastro PHI criptografado, busca por hash, LGPD export, duplicatas, import CSV.

## Rotas

- `/app/pacientes` — lista e busca
- `/app/pacientes/[id]` — ficha

## Permissões

`pacientes.view`, `pacientes.create`, `pacientes.edit`
